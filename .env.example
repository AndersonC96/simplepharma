# Application Settings
APP_NAME=SafeTickets
APP_ENV=local
APP_URL=http://localhost

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=simplepharma
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Security
SESSION_NAME=safetickets_session
# Use openssl rand -base64 32 to generate a key
CSRF_SECRET=your_random_secret_here
